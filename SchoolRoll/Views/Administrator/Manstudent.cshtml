@page
@model SchoolRoll.Views.Administrator.DelstudentModel
@{
    Layout = "_AdmLayout";
    var students = ViewBag.students as List<Student>;
}
<section class="panel">
    <form action='@Url.Action("Delstudent","Administrator")' method="post">
        <th>搜索</th>
        <th>
            <input type="text" name="student_id" value="" placeholder="请输入学号" />
        </th>
        <th>
            <input type="submit" class="btn btn-primary" value="删除" />
        </th>
    </form>
    <div>

    </div>
    <form action='@Url.Action("Showstu","Administrator")' method="post">
        <th>搜索</th>
        <th>
            <input type="text" name="Sno" value="" placeholder="请输入学号" />
        </th>
        <th>
            <input type="submit" class="btn btn-primary" value="查看" />
        </th>
    </form>
</section>
